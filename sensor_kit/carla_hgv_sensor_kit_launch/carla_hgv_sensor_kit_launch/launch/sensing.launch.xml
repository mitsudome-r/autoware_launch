<?xml version="1.0" encoding="UTF-8"?>
<launch>
  <!-- The HGV kit has the same sensors/topics as carla_sensor_kit; only the calibration differs. -->
  <arg name="vehicle_mirror_param_file" description="path to the file of vehicle mirror position yaml"/>
  <arg name="pointcloud_container_name" default="pointcloud_container"/>

  <include file="$(find-pkg-share carla_sensor_kit_launch)/launch/sensing.launch.xml">
    <arg name="vehicle_mirror_param_file" value="$(var vehicle_mirror_param_file)"/>
    <arg name="pointcloud_container_name" value="$(var pointcloud_container_name)"/>
  </include>
</launch>
